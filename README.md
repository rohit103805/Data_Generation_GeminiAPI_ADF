# Data_Generation_GeminiAPI_ADF
Data is being generated using Gemini API and the code is written in python and deployed on Azure Functions. Finally the data is being ingested by Azure Data Factory and processed as required.
